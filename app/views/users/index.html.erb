<h1>Current user</h1>

<table>
  <tr>
    <th>Username</th>
    <th>Email</th>
    <th>Password</th>
    <th>Good karma</th>
    <th>Bad karma</th>
    <th>Influence</th>
    <th>Saved</th>
    <th>Site</th>
    <th>Full name</th>

  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= user.email %></td>
    <td><%= user.password %></td>
    <td><%= user.good_karma %></td>
    <td><%= user.bad_karma %></td>
    <td><%= user.influence %></td>
    <td><%= user.saved %></td>
    <td><%= user.site %></td>
    <td><%= user.full_name %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Delete you account?', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
